namespace IdOps.IdentityServer
{
    public enum ResourceType
    {
        Client,
        ApiResource,
        IdentityResource,
        ApiScope,
        UserClaimRule,
        PersonalAccessToken
    }
}
